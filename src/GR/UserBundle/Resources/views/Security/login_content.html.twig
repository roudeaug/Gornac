{% trans_default_domain 'FOSUserBundle' %}


{% if error %}
    <div class="alert alert-danger" role="alert">
        <div>{{ error.messageKey|trans(error.messageData, 'security') }}</div>
    </div>
{% endif %}


<form class='form-horizontal' action="{{ path("fos_user_security_check") }}" method="post">
    {% if csrf_token %}
        <input type="hidden" name="_csrf_token" value="{{ csrf_token }}" />
    {% endif %}

    <label class='control-label' for="username">{{ 'security.login.username'|trans }}</label>
    <input class='form-control' type="text" id="username" name="_username" value="{{ last_username }}" required="required" />

    <label class='control-label' for="password">{{ 'security.login.password'|trans }}</label>
    <input class='form-control' type="password" id="password" name="_password" required="required" />

    <input class='col-sm-4 btn btn-primary' style="margin-top:20px" type="submit" id="_submit" name="_submit" value="{{ 'security.login.submit'|trans }}" />
</form>
